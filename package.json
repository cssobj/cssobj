{
  "name": "cssobj",
  "version": "1.1.2",
  "description": "CSS in JS solution, dynamically change and update stylesheet rules at runtime, CSSOM, localize class names, auto vendor prefixer, media query hook, styled components.",
  "keywords": [
    "cssom",
    "prefixer",
    "css generate",
    "diff css",
    "media query",
    "css modules"
  ],
  "main": "dist/cssobj.cjs.js",
  "browser": "dist/cssobj.iife.js",
  "jsnext:main": "dist/cssobj.es.js",
  "files": [
    "dist",
    "docs",
    "*.md",
    "*.ts",
    "*.json"
  ],
  "scripts": {
    "pretest": "npm run build",
    "test": "cd test && mocha",
    "lint": "standard ./lib",
    "report": "cd dist && gzip < cssobj.min.js > cssobj.min.gz && ls -lh && rm -f *.gz",
    "build": "git rev-parse HEAD > .commithash && rollup -c && uglifyjs dist/cssobj.iife.js -cm -o dist/cssobj.min.js",
    "make": "gulp bump && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cssobj/cssobj.git"
  },
  "bugs": {
    "url": "https://github.com/cssobj/cssobj/issues"
  },
  "homepage": "https://github.com/cssobj/cssobj#readme",
  "author": "James Yang <jamesyang999@gmail.com>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "chai": "*",
    "gulp": "*",
    "gulp-bump": "*",
    "mocha": "*"
    "rollup-plugin-replace": "*",
    "rollup-watch": "*",
    "walk": "*"
  }
}
